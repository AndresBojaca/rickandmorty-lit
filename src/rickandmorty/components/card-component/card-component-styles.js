import {css} from 'lit';

export const CardComponentStyles = css`
      .get-api-btn{
          background-color: var(--text-primary-color);
          text-decoration: none;
          color: var(--bg-primary-color);
          padding: .8rem;
          text-align: center;
          width: 85px;
          transition: all .3s;
          cursor: pointer;
          display: inline-block;
      }
      .get-api-btn:hover{
          opacity: 0.8
      }
      `;
